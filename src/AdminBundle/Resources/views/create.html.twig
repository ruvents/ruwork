{% extends '@RuworkAdmin/base.html.twig' %}

{% block title entity_config.create.title %}

{% block main %}
    <div class="row">
        <div class="col-auto">
            <h1>{{ entity_config.create.title }}</h1>
        </div>
    </div>

    {% form_theme form entity_config.create.theme %}
    {{ form(form) }}
{% endblock %}
