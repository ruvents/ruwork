parameters:
    package_directories:
        - src

    data_to_append:
        require-dev:
            friendsofphp/php-cs-fixer: ^2.13
            symfony/phpunit-bridge: ^3.4 || ^4.0
            symplify/monorepo-builder: ^4.6
